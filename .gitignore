# ============================================================================
# SECRETS — never commit a live keybox. A public keybox is revoked by Google.
# ============================================================================
device/data_adb/tricky_store/keybox.xml
**/keybox.xml
!**/keybox.xml.example
*.keybox

# ============================================================================
# Kernel build artifacts (regenerated by kernel-build/scripts/*)
# ============================================================================
kernel-build/sources/
kernel-build/out/
kernel-build/.ccache/
kernel-build/patches/
kernel-build/docs/
*.log

# Prebuilt / large binaries — this repo is source + scripts only.
*.img
*.img.gz
Image
Image.gz
*.tar.gz
*.zip
*.apk
*.so
*.dex

# ============================================================================
# Host / OS / editor cruft
# ============================================================================
.DS_Store
*.swp
*~
.idea/
*.iml

# Local setup cache (manager APK, module zips, KSU allowlist) — not source.
setup-cache/
